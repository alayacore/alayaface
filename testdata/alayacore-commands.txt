# command names AlayaCore's registry answers — canonical names and aliases. AlayaFace sends a subset; anything it sends that is NOT listed here comes back as UNKNOWN_COMMAND
# Source: internal/commands/commands.go (CommandName* constants, commandAliases).
# Refreshed by scripts/check-alayacore-protocol.sh whenever it runs with
# AlayaCore checked out. AlayaCore is a separate repo and absent from CI,
# so this file is what the check compares against there.
cancel
continue
fork
mcp_cancel
mcp_confirm
mcp_decline
model_load
model_set
model_sync
q
quit
reason
save
summarize
theme_set
tool_confirm
tool_decline
video_config
